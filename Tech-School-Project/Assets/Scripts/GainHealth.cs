using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GainHealth : MonoBehaviour
{
    void Start()
    {
        transform.position.y = Random.Range(-6f, 7f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
